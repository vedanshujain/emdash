{
	"lexicon": 1,
	"id": "com.emdashcms.experimental.aggregator.defs",
	"description": "Shared definitions for the EmDash plugin registry aggregator XRPC endpoints. EXPERIMENTAL: shape may change before stabilisation.",
	"defs": {
		"packageView": {
			"type": "object",
			"description": "An aggregator's view of a package, hydrating a profile record with denormalised metadata and any labels applied by trusted labellers the aggregator hydrates.",
			"required": ["uri", "cid", "did", "slug", "profile", "indexedAt"],
			"properties": {
				"uri": {
					"type": "string",
					"format": "at-uri",
					"description": "AT URI of the profile record the aggregator indexed. Pins exactly which record version this view describes."
				},
				"cid": {
					"type": "string",
					"format": "cid",
					"description": "CID of the profile record content the aggregator indexed. Lets clients confirm they're working with the same bytes the aggregator did."
				},
				"did": {
					"type": "string",
					"format": "did",
					"description": "Publisher DID. Denormalised convenience; equivalent to the DID portion of `uri`."
				},
				"slug": {
					"type": "string",
					"description": "Package slug (the rkey of the profile record). Denormalised convenience.",
					"minLength": 1,
					"maxLength": 64
				},
				"handle": {
					"type": "string",
					"format": "handle",
					"description": "Publisher's current handle, if known. Best-effort: handles are mutable and may be stale at the moment of read."
				},
				"profile": {
					"type": "unknown",
					"description": "The signed profile record verbatim, passed through from the publisher's repo (carrying its $type, all required fields, etc.)."
				},
				"latestVersion": {
					"type": "string",
					"description": "Convenience: the highest semver version among non-tombstoned, non-yanked releases the aggregator has indexed for this package. Clients SHOULD verify this against their own selection from listReleases when the difference matters.",
					"maxLength": 64
				},
				"indexedAt": {
					"type": "string",
					"format": "datetime",
					"description": "When the aggregator first indexed this package."
				},
				"historicalReleaseCount": {
					"type": "integer",
					"minimum": 0,
					"description": "Number of package release versions retained in the aggregator's history, including releases the publisher later deleted."
				},
				"releaseHistoryComplete": {
					"type": "boolean",
					"description": "Whether the aggregator continuously observed this package from its first live profile creation. When false or absent, clients must not infer that historicalReleaseCount represents the package's complete registry history."
				},
				"labels": {
					"type": "array",
					"description": "Hydrated trusted label state attached by the aggregator's configured policy.",
					"maxLength": 64,
					"items": {
						"type": "ref",
						"ref": "com.atproto.label.defs#label"
					}
				}
			}
		},
		"releaseView": {
			"type": "object",
			"description": "An aggregator's view of a release: the signed record verbatim, record-scoped blob cache services, and any hydrated labels.",
			"required": ["uri", "cid", "did", "package", "version", "release", "indexedAt"],
			"properties": {
				"uri": {
					"type": "string",
					"format": "at-uri",
					"description": "AT URI of the release record the aggregator indexed. Pins exactly which record version this view describes."
				},
				"cid": {
					"type": "string",
					"format": "cid",
					"description": "CID of the release record content the aggregator indexed."
				},
				"did": {
					"type": "string",
					"format": "did",
					"description": "Publisher DID. Denormalised convenience; equivalent to the DID portion of `uri`."
				},
				"package": {
					"type": "string",
					"description": "Parent package slug. Denormalised convenience.",
					"minLength": 1,
					"maxLength": 64
				},
				"version": {
					"type": "string",
					"description": "Release version, matching the post-':' portion of the rkey.",
					"minLength": 1,
					"maxLength": 64
				},
				"release": {
					"type": "unknown",
					"description": "The signed release record verbatim from the publisher's repo (carrying its $type and all fields)."
				},
				"artifactCaches": {
					"type": "array",
					"description": "Blob cache services the aggregator advertises for this release. Clients ignore unrecognised service variants and derive artifact URLs only for public blob refs.",
					"items": {
						"type": "union",
						"refs": ["#recordScopedBlobCache"],
						"closed": false
					},
					"maxLength": 4
				},
				"indexedAt": {
					"type": "string",
					"format": "datetime",
					"description": "When the aggregator first indexed this release."
				},
				"labels": {
					"type": "array",
					"description": "Hydrated trusted label state attached by the aggregator's configured policy.",
					"maxLength": 64,
					"items": {
						"type": "ref",
						"ref": "com.atproto.label.defs#label"
					}
				}
			}
		},
		"recordScopedBlobCache": {
			"type": "object",
			"description": "A cache that admits a blob only when an exact repository record revision references its CID. Raw URLs use /r/{did}/{collection}/{rkey}/{recordCid}/{blobCid}; image representations use /img/{preset}/r/{did}/{collection}/{rkey}/{recordCid}/{blobCid}.",
			"required": ["serviceEndpoint"],
			"properties": {
				"serviceEndpoint": {
					"type": "string",
					"format": "uri",
					"maxLength": 2048,
					"description": "HTTPS origin of the record-scoped blob cache."
				}
			}
		}
	}
}
